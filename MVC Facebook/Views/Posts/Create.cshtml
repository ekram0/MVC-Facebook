@using Microsoft.AspNetCore.Identity
@using System.Security.Claims;
@model Post
@inject UserManager<User> UserManager

@if (User.IsInRole("Normal User"))
{
<div id="postContainer" class="bg-white p-3 mb-3 rounded border">

    <div class="form-group mb-0">
        <textarea id="createPostBox" asp-for="Body" class="form-control mb-3" placeholder="What's on your mind?" style="min-height:120px; max-height:120px;"></textarea>
        <button value="Post" class="btn btn-primary col-12" onclick="addPost()" > Post </button>
    </div>

    @*<form method="post" asp-action="create" asp-controller="Posts">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PostOwnerID" value="@UserManager.GetUserId(User)" />
            <div class="form-group">
                <textarea asp-for="Body" class="form-control" placeholder="What's on your mind?" style="min-height:120px; max-height:120px;"></textarea>
                <span asp-validation-for="Body" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" id="postBtn" value="Post" class="btn btn-primary col-12" />
            </div>
        </form>*@
</div>
}
<!--Those two sections are not active because they are written in a partial view-->
@*@section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }*@
